# 🎯 1️⃣ Shader завжди працює в межах геометрії

`<Shader />` сам по собі нічого не малює.
Він лише визначає **як зафарбувати пікселі тієї геометрії, до якої він прикріплений**.

---

# 🧱 2️⃣ Правильна модель мислення

У Skia дерево працює так:

```
Geometry node
    └── Paint node
            └── Shader
```

Тобто:

```tsx
<Geometry>
  <Shader />
</Geometry>
```

— це коректна модель.

---

# 🎨 3️⃣ Приклад з Fill (весь Canvas)

```tsx
<Canvas>
  <Fill>
    <Shader ... />
  </Fill>
</Canvas>
```

`Fill` — це просто “намалюй прямокутник розміру Canvas”.

Тому Shader буде працювати на весь Canvas.

---

# 🔵 4️⃣ Приклад з обмеженою формою

```tsx
<RoundedRect x={0} y={0} width={200} height={200} r={20}>
  <Shader ... />
</RoundedRect>
```

Тепер Shader буде виконуватися **тільки для пікселів цього прямокутника**.

---

# 🟢 5️⃣ Приклад з Path (замкнута ламана)

```tsx
<Path path={someClosedPath}>
  <Shader ... />
</Path>
```

Shader буде виконуватися тільки всередині цього path.

---

# 🔥 6️⃣ Важлива тонкість

Shader **обчислюється тільки для пікселів геометрії**,
але `fragCoord` все одно містить координати у просторі Canvas.

Тобто:

- якщо ти малюєш маленький круг в центрі
- `fragCoord` все одно буде глобальним

Це дуже важливо для складних ефектів.

---

# 🧠 7️⃣ Чи обов’язково “замкнутий простір”?

Так.

Shader працює тільки там, де є геометрія з площиною.

✔ Fill — площина
✔ Rect — площина
✔ RoundedRect — площина
✔ Path (замкнутий) — площина
✔ Circle — площина

❌ Line — не площина
❌ Stroke-only без fill — працює по stroke області

---

# 📌 8️⃣ Твій синтаксис

Ти написав:

```tsx
<Компонент>
  <Shader />
</Компонент>
```

Так, це правильно — якщо `<Компонент>` є geometry node.

---

# 🧩 9️⃣ Що буде, якщо:

```tsx
<Canvas>
  <Shader ... />
</Canvas>
```

→ Нічого не намалюється.

Бо немає геометрії.

---

# 🎯 10️⃣ Тепер сформулюємо точно

✔ Shader — це фарба
✔ Geometry — це маска/площина
✔ Shader працює тільки в межах geometry
✔ Fill — це просто “весь Canvas”
✔ Обмеження визначається формою geometry

---

Ти зараз повністю правильно розумієш архітектуру.

---

Зараз ми **закриємо простір рендерингу повністю**:

1. 🔹 як `Shader` поводиться з `clip()`
2. 🔹 як `Group` впливає на область рендерингу
3. 🔹 що реально обмежує виконання shader
4. 🔹 чим це відрізняється від SDF

---

# 1️⃣ Як Shader поводиться з `clip()`

## 🔹 Правило

`clip()` обмежує область рендерингу **після того, як геометрія визначена**.

Тобто:

```tsx
<Group clip={rrect(...)} >
  <Fill>
    <Shader ... />
  </Fill>
</Group>
```

Що відбувається:

1. `Fill` хоче зафарбувати весь Canvas
2. `clip` обмежує область
3. Shader виконується **тільки в межах clip-області**

---

## 🧠 Важливо

Shader **не знає**, що він “обрізаний”.
Він просто не викликається для пікселів поза clip.

`fragCoord` залишається глобальним.

---

# 2️⃣ Як `Group` впливає на область рендерингу

`Group` сам по собі **не обмежує область**.

Він:

- групує вузли
- дозволяє застосувати transform
- дозволяє застосувати clip
- дозволяє застосувати opacity

---

### Приклад без clip:

```tsx
<Group transform={[{ translateX: 100 }]}>
  <Fill>
    <Shader ... />
  </Fill>
</Group>
```

Shader буде виконуватись на весь Canvas,
але координати будуть трансформовані.

---

### Приклад з clip:

```tsx
<Group clip={rect(0,0,200,200)}>
  <Fill>
    <Shader ... />
  </Fill>
</Group>
```

Shader буде виконуватись тільки в 200x200 області.

---

# 3️⃣ Що реально визначає область виконання shader

Порядок такий:

```
Geometry → Transform → Clip → Shader
```

Область виконання =

```
(Геометрія) ∩ (Clip)
```

---

## Приклад 1

```tsx
<RoundedRect width={200} height={200}>
  <Shader />
</RoundedRect>
```

Область = 200x200 округлений прямокутник.

---

## Приклад 2

```tsx
<Group clip={circle}>
  <RoundedRect width={200} height={200}>
    <Shader />
  </RoundedRect>
</Group>
```

Область = перетин RoundedRect та circle.

---

# 4️⃣ Дуже важлива річ про `fragCoord`

Навіть якщо ти обрізав область, `fragCoord`:

- все одно в координатах Canvas
- не локальний до геометрії

Тобто якщо круг в центрі — `fragCoord` не починається з (0,0) в межах круга.

Це часто плутає.

---

# 5️⃣ Чи виконується shader “для всього екрану”?

Ні.

GPU оптимізує:

Shader викликається тільки для пікселів, які реально треба намалювати.

---

# 6️⃣ Stroke vs Fill

Якщо ти робиш:

```tsx
<RoundedRect style="stroke">
  <Shader />
</RoundedRect>
```

Shader буде працювати тільки на товщині stroke.

---

# 7️⃣ Тепер головне — відмінність від SDF

Зараз ти:

- обмежуєш форму геометрією Skia
- Shader тільки зафарбовує її

У SDF ми:

- НЕ використовуємо RoundedRect
- НЕ використовуємо Path
- Малюємо на весь Canvas
- А форму визначаємо математично всередині shader

Наприклад:

```glsl
float d = length(uv - 0.5);
if (d < 0.3) {
    return half4(1,0,0,1);
}
return half4(0,0,0,1);
```

Тут круг створений математично, без геометрії.

---

# 8️⃣ Резюме (закриваємо тему простору)

✔ Shader працює тільки в межах геометрії
✔ Clip додатково обмежує область
✔ Group без clip не обмежує
✔ fragCoord глобальний
✔ Область = Geometry ∩ Clip
✔ Fill = геометрія “весь Canvas”
✔ SDF = форма створюється математично

---

# 🎓 РОЗДІЛ 1 — Структура мови SkSL

---

# 1️⃣ Що таке SkSL з точки зору мови

SkSL — це:

- C-подібна мова
- типізована
- компілюється в GPU-байткод
- виконується для кожного пікселя
- є підмножиною / адаптацією GLSL

---

# 2️⃣ Базова структура файлу

Кожен fragment shader у Skia має таку форму:

```glsl
uniform ...;

half4 main(float2 fragCoord) {
    ...
    return half4(...);
}
```

Це обов'язкова сигнатура для RuntimeEffect.

---

# 3️⃣ Сигнатура main

```glsl
half4 main(float2 fragCoord)
```

### 🔹 float2 fragCoord

Координати пікселя.

### 🔹 half4

Повертає колір RGBA.

---

# 4️⃣ Типи даних (базові)

## Скалярні типи

| Тип   | Опис                   |
| ----- | ---------------------- |
| float | 32-bit                 |
| half  | 16-bit (оптимізований) |
| int   | ціле                   |
| bool  | логічний               |

---

## Векторні типи

| SkSL   | GLSL |
| ------ | ---- |
| float2 | vec2 |
| float3 | vec3 |
| float4 | vec4 |
| half2  | vec2 |
| half4  | vec4 |

---

### Приклад

```glsl
float x = 0.5;
float2 v = float2(0.2, 0.8);
float3 color = float3(1.0, 0.0, 0.0);
```

---

# 5️⃣ Створюємо перший академічний приклад

---

## 📁 КРОК 1 — створити файл

Створи:

```
src/shaders/lesson1_types.sksl
```

---

## 📄 lesson1_types.sksl

```glsl
uniform float2 u_resolution;

half4 main(float2 fragCoord) {

    float x = 0.5;
    float y = 0.2;

    float2 v = float2(x, y);

    return half4(v.x, v.y, 0.0, 1.0);
}
```

---

## 📥 КРОК 2 — імпорт

У App:

```tsx
import lesson1 from "./src/shaders/lesson1_types.sksl";
```

---

## 🧠 КРОК 3 — використовуємо ShaderCanvas

```tsx
import { ShaderCanvas } from "./ShaderCanvas";

export default function App() {
  return <ShaderCanvas source={lesson1} />;
}
```

---

## 🎯 Очікуваний результат

Екран має бути:

- червоний на 50%
- зелений на 20%
- синій 0%

Тобто темно-жовтуватий.

---

# 6️⃣ Оператори

SkSL підтримує:

```
+  -  *  /
```

Для векторів працює поелементно.

---

## 📁 Створи lesson2_math.sksl

```glsl
uniform float2 u_resolution;

half4 main(float2 fragCoord) {

    float2 uv = fragCoord / u_resolution;

    float value = uv.x * uv.y;

    return half4(value, value, value, 1.0);
}
```

---

## Що тут відбувається

- множимо X на Y
- отримаєш темніший кут зліва
- світліший правий верхній

---

# 7️⃣ Ключові слова

## if

```glsl
if (condition) {
}
```

## else

## return

## uniform

## const

---

## 📁 lesson3_if.sksl

```glsl
uniform float2 u_resolution;

half4 main(float2 fragCoord) {

    float2 uv = fragCoord / u_resolution;

    if (uv.x > 0.5) {
        return half4(1.0, 0.0, 0.0, 1.0);
    }

    return half4(0.0, 0.0, 1.0, 1.0);
}
```

---

## Результат

- ліва половина синя
- права половина червона

---

# 8️⃣ Вбудовані функції

| Функція      | Опис                 |
| ------------ | -------------------- |
| length()     | довжина вектора      |
| dot()        | скалярний добуток    |
| normalize()  | нормалізація         |
| abs()        | модуль               |
| min()        | мінімум              |
| max()        | максимум             |
| clamp()      | обмеження            |
| mix()        | лінійна інтерполяція |
| smoothstep() | плавний перехід      |

---

## 📁 lesson4_functions.sksl

```glsl
uniform float2 u_resolution;

half4 main(float2 fragCoord) {

    float2 uv = fragCoord / u_resolution;

    float d = distance(uv, float2(0.5, 0.5));

    return half4(d, d, d, 1.0);
}
```

---

# 9️⃣ Підсумок першого розділу

Ти вже знаєш:

✔ структура файлу
✔ типи
✔ вектори
✔ оператори
✔ if
✔ базові функції

---

# 📌 Що далі

Наступний блок:

🎓 Векторна математика (детально)

- swizzling (.xy, .yx, .xxx)
- поелементні операції
- матриці
- координатні системи

---

Перш ніж рухатися далі:

🔹 Створи всі 4 файли
🔹 Переконайся що вони запускаються
🔹 Спробуй змінити числа

Коли освоїшся — напиши.
Тоді підемо глибше в мову.
